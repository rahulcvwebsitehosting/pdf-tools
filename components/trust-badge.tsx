export function TrustBadge() {
  return (
    <div className="neon-badge w-full text-center px-4 py-3.5 text-xs sm:text-sm font-bold border border-border uppercase tracking-wider">
      🔒 100% SECURE SANDBOX LOCAL COMPUTING: Data is processed inside memory frames on your machine. Zero network requests made.
    </div>
  );
}
